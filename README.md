# Private Blog by Hugo and Stack

## Preconduct Environment
1. Install Hugo and Go
2. git clone this project
3. Download theme modules: `hugo mod tidy`
4. `hugo server -D` / `hugo server --bind 0.0.0.0 --baseURL http://10.20.173.8:1313 -D`
